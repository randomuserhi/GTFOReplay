interface GTFOSpecification
{
    scale: number,
    player: {
        maxHealth: number
    }
}

let GTFOSpecification: GTFOSpecification = {
    scale: 30,
    player: {
        maxHealth: 25
    }
}