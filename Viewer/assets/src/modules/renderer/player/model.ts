import { StickFigure } from "../models/stickfigure";

export class PlayerModel extends StickFigure {
    constructor() {
        super();
    }

    public render(dt: number, time: number, metadata: T) {

    }
}